testing
isOnlyUpdateIfCurrent 
	"Answer whether the repository must ensure the object's version is current before any update of such object, i.e. only update if the object hasn't a newer version in the db."

	^onlyUpdateIfCurrent